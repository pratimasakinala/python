# import urllib.request
# import json
#
# url = 'http://ipinfo.io/json'
#
# response = urllib.request.urlopen(url)
# data = response.read().decode('utf-8')
# d = json.loads(data)
#
# print('You are {ip} in {city}'.format(**d)) # unpack keys as individual parameter by using **


# easier way of doing this is with requests library
